#include <stdio.h>
int main() {
    int a, prm,b;
   printf("give the number you want to check for prime: ");
   scanf("%d",&prm);

   for(a=prm;a%prm==1;b++)

    printf("%d is prime");


    return 0;
}
